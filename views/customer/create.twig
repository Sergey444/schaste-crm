{#
/* @var $this yii\web\View */
/* @var $model app\models\Personal */
#}
{{ set(this, 'title', lang('app', 'Create contact')) }}
{{ set(this, 'params', { 'breadcrumbs' : [{ 'label' : lang('app', 'Contacts'), 'url': '/customer' }, {'label' : this.title }] }) }}

<div class="personal-create">

    <div class="bg-white">
    <h3>Страница добавления контакта</h3><hr />
    {{ this.render('_form.twig', {
        'model': model,
    }) | raw }}
    </div>
</div>