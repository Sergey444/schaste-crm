{#
/* @var $this yii\web\View */
/* @var $model app\models\Dh */
#}
{{ set(this, 'title', lang('app', 'Update record')) }}
{{ set(this, 'params', { 'breadcrumbs' : [{ 'label' : lang('app', 'Domens and hostings'), 'url': path(['index']) }, {'label' : this.title }] }) }}

<div class="dh-update">

    <div class="bg-white">
        <h3>Страница редактирования записи</h3><hr />
        {{ this.render('_form.twig', {
            'model': model,
        }) | raw }}
    </div>

</div>