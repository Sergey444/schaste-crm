{#
/* @var $this yii\web\View */
/* @var $model app\models\PaymentIn */
#}

{{ set(this, 'title', lang('app', 'Update payment in')) }}

{{ set(this, 'params', { 'breadcrumbs' : [{ 'label' : lang('app', 'Payments'), 'url': '/payment' }, {'label' : this.title }] }) }}

<div class="payment-in-update">
    <div class="bg-white">
        <h3>Страница редактирования оплаты</h3><hr />
        {{ this.render('_form-in.twig', {model: model}) | raw}}
    </div>
</div>

